services:
  frontend:
    ports: []
    expose:
      - "80"

  backend:
    ports: []
    expose:
      - "5000"

  mailpit:
    ports: []
    expose:
      - "1025"
      - "8025"

  nginx:
    profiles:
      - disabled

networks:
  default:
    name: ttc-network
    driver: bridge
