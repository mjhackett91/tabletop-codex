# Table Top Codex - Docker Compose Environment Variables
# Copy this file to .env and customize for your deployment
# DO NOT commit .env to Git (already in .gitignore)

# ============================================
# Application Configuration
# ============================================
NODE_ENV=production
APP_NAME=Table Top Codex

# ============================================
# Network & Port Configuration
# ============================================
# Change these to match your NAS network setup
# Example: If your NAS IP is 192.168.1.100, set FRONTEND_URL=http://192.168.1.100

# Frontend URL (how users will access your app)
# For local network: http://192.168.1.100 (replace with your NAS IP)
# For domain: https://yourdomain.com
FRONTEND_URL=http://localhost

# Port mappings (change if ports conflict with other services on your NAS)
BACKEND_PORT=5000          # Backend API port (external)
FRONTEND_PORT=80           # Frontend web port (external)
NGINX_PORT=8080            # Nginx reverse proxy port (if used)
POSTGRES_PORT=5432         # PostgreSQL port (external, usually keep internal-only)
MAILPIT_SMTP_PORT=1025     # Mailpit SMTP port (usually internal-only)
MAILPIT_UI_PORT=8025       # Mailpit web UI port (external - view emails here)

# ============================================
# Database Configuration
# ============================================
# PostgreSQL connection settings
POSTGRES_DB=ttc
POSTGRES_USER=ttc_user
POSTGRES_PASSWORD=ttc_password_change_in_production

# Full database URL (alternative to individual settings above)
# DATABASE_URL=postgresql://ttc_user:ttc_password@postgres:5432/ttc

# ============================================
# Security
# ============================================
# JWT Secret - REQUIRED in production
# Generate a strong secret: openssl rand -base64 32
JWT_SECRET=change-me-in-production

# CORS Configuration (for production with custom domain)
# Comma-separated list of allowed origins
# Example: ALLOWED_ORIGINS=https://yourdomain.com,https://www.yourdomain.com
ALLOWED_ORIGINS=

# ============================================
# Email Configuration (for password reset)
# ============================================
# Mailpit is already configured in docker-compose.yml (no setup needed)
# To customize email sender:
EMAIL_FROM=noreply@tabletopcodex.local

# If you want to use a different email service instead of Mailpit, uncomment and configure:

# Gmail:
# EMAIL_SERVICE=gmail
# EMAIL_USER=your-email@gmail.com
# EMAIL_PASSWORD=your-gmail-app-password
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587

# SendGrid:
# EMAIL_SERVICE=sendgrid
# SENDGRID_API_KEY=SG.your-api-key-here
# SMTP_HOST=smtp.sendgrid.net
# SMTP_PORT=587

# Custom SMTP:
# SMTP_HOST=smtp.example.com
# SMTP_PORT=587
# SMTP_SECURE=false
# SMTP_USER=username
# SMTP_PASSWORD=password

# ============================================
# Build Configuration
# ============================================
# Frontend API URL (used during build - usually keep default for Docker)
VITE_API_URL=http://backend:5000/api
